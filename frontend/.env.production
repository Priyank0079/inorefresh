# Production build environment (used by `vite build`).
# Local development keeps using .env (localhost). This file guarantees that a
# production build NEVER ships a localhost API URL.
VITE_API_BASE_URL=https://api.inorfresh.com/api/v1
